# Hiperparámetros del modelo (versionados con Git)
model:
  n_estimators: 100
  max_depth: 10
  min_samples_split: 5
  learning_rate: 0.1

data:
  test_size: 0.2
  random_state: 42
  scale_features: true

evaluation:
  primary_metric: "rmse"
  threshold_rmse: 0.5  # Si RMSE > 0.5, no promover